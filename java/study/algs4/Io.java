package algs4;

import java.io.*;
public class Io {
  public static void main(String[] args) throws IOException {
    // var input = new DataInputStream(new FileInputStream("in.txt"));
    // var output = new DataOutputStream(new FileOutputStream("out.txt"));
    // while (input.available() > 0) {
    //   output.writeChar(input.readChar());
    // }

    // var file = new File("in.txt");
    // if (file.exists()) {
    //   var input = new Scanner(file);
    //   while (input.hasNext()) {
    //     System.out.println(input.nextLine());
    //   }
    // } else {
    //   System.out.println("File does not exist");
    // }
  }
}